name: ASCII Workflow

on:
  - push

jobs:
    first_job:
        name: ASCII Workflow
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v4
            - name: Execute ASCII Workflow Shell Script
              run: |
                chmod +x ./ascii-script.sh
                ./ascii-script.sh
